
## 2026-09-05 对话 UI 尺寸优化
- 已定位 `scenes/dialogue_ui.tscn`：面板固定 218px 高，内容区固定 100px，控件宽度硬编码。
- 计划改为更高的底部面板，并使用锚点/偏移实现窗口宽度适配。
- 已将面板高度调整为 320px，文本字号提升至 18px，输入行与按钮改为右侧锚点布局。
- 验证完成：`godot --headless --path helloagents-ai-town --editor --quit` 成功退出；仅报告项目既有资源大小写/UID 警告。
- `dialogue_ui.tscn` 布局检查通过，未修改对话脚本行为。

## 2026-09-05 对话显示与好感度修复
- 修复对话文本和输入框缺少右/下锚点导致的负高度与极窄宽度。
- 后端 `/chat` 响应新增 `affinity`，前端在情绪摘要旁显示 `好感度 x/100`。
- 独立好感度模型配置改为与当前可用主模型一致，并增强 Markdown JSON 响应解析。
- 待验证：Godot 场景加载、Python 语法检查。
- 验证完成：Python `py_compile` 通过；Godot headless 场景加载成功，仅有项目既有资源大小写/UID 警告。

## 2026-09-05 情绪与好感度文案
- 将状态栏改为“情绪：标签（强度 x/100） 好感度：y/100”，明确区分两个指标。
- 补充情绪数据为空时的默认好感度显示。
